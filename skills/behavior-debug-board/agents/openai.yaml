interface:
  display_name: "Behavior Debug Board (legacy)"
  short_description: "Legacy alias that routes existing prompts to Difftale"
  default_prompt: "Use $behavior-debug-board as the legacy alias and continue this request with the Difftale workflow."
policy:
  allow_implicit_invocation: false
