interface:
  display_name: "Behavior Debug Board"
  short_description: "Create, save, and Git-version local behavior debug boards"
  default_prompt: "Use $behavior-debug-board to save this bug as a local before/after board and ask whether I want Git version control."
dependencies:
  tools:
    - type: "mcp"
      value: "thesvg"
      description: "Search and fetch exact brand or service SVG logos"
policy:
  allow_implicit_invocation: true
